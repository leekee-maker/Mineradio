# 技术选型约定

- 涉及 3D、渲染引擎、WebView/原生实现、外部服务或新增依赖等重要技术选型，先向用户说明方案与取舍并征得同意后实施。
- 用户已引入或指定 Three.js 时，3D 效果默认优先使用 Three.js；若不适用，先说明原因并获得同意。
- 不因“性能优化”自行降级为 2D/CSS/Canvas 方案。

# Git 提交约定

- 每次完成任何代码/文件改动后，必须做一次本地 git commit，并写明清晰的 commit 注释（说明改了什么、为什么改）。
- 只做本地提交即可，除非用户明确要求，否则不自动推送到远程。
